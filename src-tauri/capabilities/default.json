{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lets the overlay window receive engine state events. Without a capability granting core:event, listen() is denied by the ACL and the overlay never leaves its idle state.",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}

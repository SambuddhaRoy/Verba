target/
models/*.bin
src-tauri/.tools/
# Regenerated by tauri-build on every compile; 4.5k lines of diff noise.
src-tauri/gen/schemas/
node_modules/
dist/
*.db
# Throwaway harnesses: a page with its Tauri calls stubbed, used to exercise a
# window's render paths outside the app. Named with a leading underscore so the
# pattern catches them all.
src/_*.html
